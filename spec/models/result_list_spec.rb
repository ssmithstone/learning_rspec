require 'spec_helper'

describe ResultList do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ResultList.create!(@valid_attributes)
  end
end
