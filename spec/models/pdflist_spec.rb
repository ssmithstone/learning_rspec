require 'spec_helper'

describe Pdflist do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Pdflist.create!(@valid_attributes)
  end
end
